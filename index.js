const getTodosSync = () => {};

const getTodoSync = (id) => {};

const createTodoSync = (todo) => {};

const updateTodoSync = (id, updates) => {};

const deleteTodoSync = (id) => {};

module.exports = {
  getTodosSync,
  getTodoSync,
  createTodoSync,
  updateTodoSync,
  deleteTodoSync,
};
